{
  "name": "@coadvisor/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Authentification CoAdvisor — abstraction AuthProvider, implémentation auto-hébergée (sessions + Argon2), RBAC",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@coadvisor/database": "workspace:*",
    "@coadvisor/types": "workspace:*",
    "argon2": "^0.41.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@coadvisor/config": "workspace:*",
    "@types/node": "^22.10.2",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.2"
  }
}
